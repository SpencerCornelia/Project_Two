<div>
	<h3>Hello, <%= @user.name %></h3>
</div>
<hr>

<%= render partial: 'rooms/form', locals: {room: @room} %>

<div class="col-xs-12">
	<h4>Your Current Rooms</h4>
	<% @user.rooms.each do |r| %>
		<div>
		<h5><%= link_to r.name, room_path(r), class: "col-sm-4" %></h5>
		</div>
	<% end %>
</div>


